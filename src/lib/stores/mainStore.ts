import { writable } from 'svelte/store';
import type { Writable } from 'svelte/store';
export { Writable };

/*
type Name ={
	attribute: type;
}

type MainStore = {
	name: Name;
};

const mainSotre: Writable<MainStore> = writable({
	name: {
		attribute: ''
	}
});
export default mainStore;
*/